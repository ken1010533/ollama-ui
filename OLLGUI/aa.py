import 模塊.可用模型版本 as 可用版本  # 載入語言設定模塊

# 設定模型名稱並取得模型版本網址
預設1模型 = "gemma3"  # 手動設置預設模型名稱為 'qwq'，避免它成為函式
模型版本網址 = f"https://ollama.com/library/{預設1模型}/tags"
版本列表, _, _ = 可用版本.搜尋模組版本(模型版本網址)

# 使用搜尋模組版本函數來獲取資料
版本列表 = 可用版本.搜尋模組版本(模型版本網址)

# 確保版本列表的第一項是我們需要的資料
# 確保版本列表的第一項是我們需要的資料
if isinstance(版本列表, tuple) and len(版本列表) == 3:
    版本資料 = 版本列表[0]  # 取得模型版本資料（列表）
    計數 = 版本列表[1]  # 取得計數
    模型版本網址 = 版本列表[2]  # 取得網址

    # 現在開始處理版本資料
    if 版本資料:
        for 版本 in 版本資料:
            if isinstance(版本, tuple) and len(版本) == 4:
                名稱, 編號, 容量, 發佈時間 = 版本
                print(f"名稱: {名稱}")
                print(f"編號: {編號}")
                print(f"容量: {容量}")
                print(f"發佈時間: {發佈時間}")
                print("--------")
            else:
                print("找到非預期的資料格式:", 版本)


    print(f"目前共找到 {計數} 筆模型版本資料")
    print("--------")
    print(模型版本網址)  # 印出網址
else:
    print("版本列表格式錯誤:", 版本列表)




